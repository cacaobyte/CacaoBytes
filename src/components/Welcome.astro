---
import logo from '../assets/CacaoByte.png';
import { services, advantages } from '../Models/Welcome/welcomeModels';
---

<!-- Hero Section -->
<section class="relative flex flex-col items-center justify-center min-h-screen text-center bg-gray-100 text-gray-900 px-6">
  <div class="absolute inset-0 bg-[url('/fondo.jpg')] bg-cover bg-center opacity-20"></div>

  <div class="relative w-full max-w-4xl mx-auto p-8 bg-white bg-opacity-90 rounded-xl shadow-xl animate-fade-in">
    <img src={logo.src} alt="CacaoByte Logo" class="w-32 mx-auto mb-6" />

    <h1 class="text-5xl font-bold text-gray-900 leading-tight">
      Innovamos, mejoramos y Elevamos tu Negocio con <span class="text-[#D12A40]">Tecnología</span>
    </h1>

    <p class="text-lg text-gray-700 mt-4">
      En <strong>CacaoByte S.A.</strong>, no solo creamos soluciones, 
      brindamos <strong>experiencia, innovación y eficiencia</strong> para llevar tu empresa al siguiente nivel.
    </p>

    <p class="mt-4 text-xl font-semibold text-gray-800">
      "Soluciones digitales para el presente, innovación para el futuro."
    </p>

    <div class="mt-6 flex flex-col md:flex-row gap-4 justify-center">
      <a href="/servicios" class="px-6 py-3 bg-[#D12A40] hover:bg-[#B92338] text-white rounded-lg shadow-lg transition-all transform hover:scale-105">
        🚀 Nuestros Servicios
      </a>
      <a href="/contact/contact/" class="px-6 py-3 bg-gray-800 hover:bg-gray-700 text-white rounded-lg shadow-lg transition-all transform hover:scale-105">
        📩 Contáctanos
      </a>
    </div>
  </div>
</section>

<!-- Sección de Servicios -->
<section class="py-16 bg-gray-50 text-gray-900">
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-4xl font-bold text-gray-800">Nuestros Servicios</h2>
    <p class="text-gray-600 mt-2">Innovación, Eficiencia y Seguridad en Soluciones Digitales</p>
    
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 mt-12">
      {services.map(service => (
        <div class={`relative p-6 bg-white rounded-xl shadow-lg border-2 border-transparent transition-all hover:border-opacity-100 hover:shadow-xl transform hover:-translate-y-2 ${service.color}`}>
          <div class="flex items-center gap-4">
            <span class={`text-4xl ${service.color}`}>{service.icon}</span>
            <h3 class="text-2xl font-semibold text-gray-800">{service.title}</h3>
          </div>
          <p class="text-gray-600 mt-2">{service.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>


<!-- Sección de Ventajas -->
<section class="py-16 bg-white text-gray-900 ">
  <div class="max-w-6xl mx-auto text-center">
    <h2 class="text-4xl font-bold text-gray-800">¿Por qué elegir CacaoByte?</h2>
    <p class="text-gray-600 mt-2">Más que una empresa de software, somos tu socio estratégico.</p>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12 py-4">
      {advantages.map(advantage => (
        <div class={`p-6 bg-white rounded-xl shadow-lg border-2 border-transparent transition-all hover:border-opacity-100 hover:shadow-xl transform hover:-translate-y-2 ${advantage.color}`}>
          <div class="flex items-center gap-4">
            <span class={`text-4xl ${advantage.color}`}>{advantage.icon}</span>
            <h3 class="text-2xl font-semibold text-gray-800">{advantage.title}</h3>
          </div>
          <p class="text-gray-600 mt-2">{advantage.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
